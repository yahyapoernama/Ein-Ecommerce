import 'package:flutter/material.dart';

class CustomToastification extends StatelessWidget {
  const CustomToastification({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('To Be Implemented'),
    );
  }
}
